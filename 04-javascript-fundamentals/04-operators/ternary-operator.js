// Ternary (Conditional) Operator 

// Syntax: condition ? exprIfTrue : exprIfFalse

let age = 19

let isEligible = (age >= 18) ? "Eligible" : "Not Eligible" ;

console.log("student is ",isEligible," to vote");
