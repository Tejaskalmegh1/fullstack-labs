"use strict"; // treat all js code as newer version

let studentName = "Tejas";               // string
const rollNumber = 77;                   // number
let applicationNumber = 12345678910;     // bigint
let isLoggedIn = true;                   // boolean
let score;                               // undefined
let assignment = null;                   // null

// type of a null data type is object

console.log(typeof studentName);
console.log(typeof rollNumber);
console.log(typeof applicationNumber);
console.log(typeof isLoggedIn);
console.log(typeof score);
console.log(typeof assignment);
